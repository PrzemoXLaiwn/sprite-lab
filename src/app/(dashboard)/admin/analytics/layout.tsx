export const metadata = {
  title: "Analytics Dashboard | SpriteLab Admin",
  description: "Monitor generation quality and learn from user feedback",
};

export default function AnalyticsLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
